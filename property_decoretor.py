# @property = This is a decorator that allows you to define a method as a property.
# It allows you to access a method as if it were an attribute, without needing to call it like a function.
# This is useful for encapsulating data and providing a clean interface for accessing attributes.
# Gives you setter , getter and deleter methods